{
  "name": "text1",
  "version": "1.0.0",
  "description": "play",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --index index.html --files \"*.html,js/*.js,css/*.css\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
